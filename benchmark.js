const sheet = SpeadsheetApp.getActiveSpreadsheet().getSheetByName('Mar 2023');
const range = 'AB3:AA';
const rawVals = sheet.getRange(range).getValues();
const vals = vals.map((val, index) => {
	val[1];
});
